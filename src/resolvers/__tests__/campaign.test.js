/* eslint-disable no-unused-vars */
const { Query, Mutation } = require('../campaign');

describe('campaign resolver', () => {
  describe('query', () => {
    it('should be a placeholder', async () => {
      expect(1).toEqual(1);
    });
  });

  describe('mutation', () => {
    it('should be a placeholder', async () => {
      expect(1).toEqual(1);
    });
  });
});
