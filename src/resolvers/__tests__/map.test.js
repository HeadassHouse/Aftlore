const { Query, Mutation } = require('../map');

describe('map resolver', () => {
  describe('query', () => {
    it('should be a placeholder', async () => {
      expect(1).toEqual(1);
    });
  });

  describe('mutation', () => {
    it('should be a placeholder', async () => {
      expect(1).toEqual(1);
    });
  });
});
