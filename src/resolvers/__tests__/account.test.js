const { Query, Mutation } = require('../account');

describe('account resolver', () => {
  describe('query', () => {
    it('should be a placeholder', async () => {
      expect(1).toEqual(1);
    });
  });

  describe('mutation', () => {
    it('should be a placeholder', async () => {
      expect(1).toEqual(1);
    });
  });
});
