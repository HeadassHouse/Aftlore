# import '../utils/utils.graphql'
type Ability {
    _id: ID
    name: String
    uses: Int
}

input AbilityInput {
    name: String!
    uses: Int!
}

input AbilityEdit {
    name: String
    uses: Int
}
