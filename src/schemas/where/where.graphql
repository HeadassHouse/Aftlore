enum Operation {
    EQUALS
    CONTAINS
    LT
    LTE
    GT
    GTE
}

input RequestFilter {
    operation: Operation
    value: String
    property: String
    subProperty: RequestFilter
}

input Or {
    or: [ RequestFilter ]
}

input And {
    and: [ Or ]
}