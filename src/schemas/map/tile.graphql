enum Terrain {
    IMPASSABLE
    PASSABLE
    ROUGH
    SLIPPERY
}

input TileInput {
    x: Int
    y: Int
    height: Int
    width: Int
    type: Terrain
}

type Tile {
    _id: ID
    x: Int
    y: Int
    height: Int
    width: Int
    type: Terrain
}