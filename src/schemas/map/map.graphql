# import * from './tile.graphql'
# import * from '../where/where.graphql'
# import * from '../statusCode.graphql'
# import * from '../updateAny/updateAny.graphql'

input MapInput {
    imageLink: String!
    name: String!
    tiles: [TileInput]
}

input MapUpdate {
    imageLink: String
    name: String
    tiles: [TileInput]
}

type Map {
    _id: ID
    imageLink: String
    name: String
    tiles: [Tile]
}

type Query {
    getMap(id: String, where: And): [Map]
}

type Mutation {
    createMap(map: MapInput!): StatusCode
    editMap(_id: ID!, update: MapUpdate!): Map
    deleteMap(_id: ID!): StatusCode
}

type Subscription {
  mapUpdated: Map
}